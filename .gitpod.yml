tasks:
  - before: cd /home/gitpod/.nvm/versions/node/*/lib/node_modules/node-red/ && npm link /workspace/openapi-red/ && cd /workspace/openapi-red/
image:
  file: .gitpod.Dockerfile
vscode:
  extensions:
    - svelte.svelte-vscode